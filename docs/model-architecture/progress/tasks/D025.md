# D025: Govern production data assembly

Status: explicit-spawn production boundary, process-local accepted stream, and real
AF_UNIX service replay integration passed; independent DATA rereview pending.

## Allowed paths

- `config/examples/all_options.example.toml`
- `src/sakuramoon/config/schema.py`
- `src/sakuramoon/data/__init__.py`
- `src/sakuramoon/data/production.py`
- `src/sakuramoon/data/validation.py`
- `tests/unit/config/test_d025_data_assembly.py`
- `tests/unit/data/test_production.py`
- `tests/unit/data/test_d024_collate.py`
- `tests/gpu/data/test_d024_service_consumer.py`
- `tests/gpu/data/test_pipeline_encoders.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/D025.md`
- `docs/model-architecture/reviews/D025/**`

`docs/model-architecture/progress/IMPLEMENTATION_ROADMAP.md` contains a separate
user worktree change and is deliberately excluded from D025.

## Contract

- The production ModelScope metadata adapter and caption parser are module-level,
  governed data APIs rather than helpers defined only by a GPU test. They preserve
  D021's trusted `ShardRecord`, explicit metadata mapping, original-JSON caption
  parsing, and D014 caption/image/collate semantics.
- The validation exclusion set is loaded from the configured JSONL only after exact
  SHA-256, 2,000-row cardinality, canonical field/type/encoding, sorted-ID, and
  global-uniqueness validation. Missing files, symlinks, digest drift, malformed
  rows, and identity drift hard fail before worker construction.
- `stage.local_batch`, `data.cache.persistent_workers_per_rank`,
  `data.cache.ready_batches_per_rank`, `data.loader.pin_memory`, and
  `data.loader.drop_last` are the only loader-control sources. The production loader
  accepts a resolved `RuntimeConfig`, rejects service topology drift, and exposes no
  independent value arguments or fallback topology.
- The production factory and every generated pipeline must serialize through the
  exact `ForkingPickler` used by the explicit `spawn` context before worker launch.
  Local functions, lambdas, and other unpickleable tokenizer/observer fields hard
  fail; there is no fallback to `fork` or one worker.
- `ProductionPipelineFactory.batches()` returns only a factory-issued,
  process-local `AcceptedProductionBatchStream`. Its immutable identity binds the
  full resolved-config SHA-256, all five resolved loader controls, exact manifest
  SHA-256, service-session SHA-256, worker count, and a unique 256-bit factory
  identity. Plain iterators, caller-built handles, factory/manifest/topology drift,
  cross-process use, and serialization hard fail.
- D024 continues to own network, download, verification, cache, `mainset`, lease,
  ACK, and replay state. D025 imports none of the service-owned transport/cache
  implementation and does not alter completion or restart semantics.

## Evidence boundary

The targeted selector passed 13 CPU/config/data tests. A real independent AF_UNIX
service process supplied four valid tar shards through the D025 factory to two
distinct spawned DataLoader worker PIDs; all four matching leases were acknowledged
only after normal exhaustion. Early parent close and real worker image-decode failure
each left exactly two leases active with zero ACKs; a fresh service replayed exactly
those two shards from tar start and persisted exactly two replayed shards/two samples.

D023/D024 worker, service, and fault adjacency passed 22 tests after replacing old
test-only local lambdas with module-level spawn-serializable equivalents. Targeted
Ruff and strict Pyright pass. Main-agent live trace verification passed all 237
requirements/source nodes; independent DATA rereview remains pending. The existing
real-shard GPU test now consumes the governed production parser; no new long run,
network/storage sweep, formal stage, DDP/NCCL, or multi-GPU claim is made by D025.
