# M035: JLT upstream alignment and evidence qualification

Status: documentation/evidence implementation and targeted validation complete;
independent Dense remediation review pending. Implementation commit reference:
`task:M035`.

## Dependencies

- M033 strict JLT remediation commit
  `7a36605fb17235580262b76cab07181076aad459` is complete.
- M034 sampling-profile commit
  `c8ca285eaab7f6d9c148baa5ca4d700f470105c7` is unchanged and outside this task.
- R001 locks JLT to commit
  `aca236efa97aab3b7d865fd3d99a270431cf6ae5`.

## Allowed paths

- `docs/model-architecture/progress/tasks/M033.md`
- `docs/model-architecture/progress/tasks/M035.md`
- `docs/model-architecture/progress/traceability.toml`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/reviews/M035/upstream_algorithm_matrix.md`
- `docs/model-architecture/reviews/M035/test_report.json`

## Scope

- Statically compare the locked JLT commit with the current strict JLT objective and
  CFG implementation at the level of formulas, clamp branches, reduction, dtype,
  timestep direction, and guidance order.
- Record every intentional difference where current confirmed decisions override the
  upstream reference.
- Qualify the existing M033 one-GPU evidence as a component CUDA smoke because it uses
  a synthetic parameter, SGD, and a synthetic velocity function rather than the
  production DiT/training call chain.
- Append M035 task and evidence paths to the existing `C08-001` through `C08-004`
  trace mappings without changing their IDs, fingerprints, historical evidence, or
  implementation commit references.

## Non-goals

- Do not import, execute, or make production/tests depend on `reference/`.
- Do not change the strict JLT runtime, objective/sampler behavior tests, sampler,
  sampling profiles, configuration, current architecture documents, or historical
  M033 evidence. The trace fixture may only add the new M035 evidence paths.
- Do not claim a real data/Qwen/VAE/DiT/loss/checkpoint training integration. T050
  retains ownership of that one-to-ten-step engineering smoke.
- Do not claim DDP, NCCL, multi-GPU, long-run, or formal-stage evidence.

## Validation

- Run the live trace verifier and the traceability contract suite.
- Run `git diff --check` and confirm the diff is restricted to the allowed paths.
- An independent Dense remediation reviewer must separately assess AI/model
  correctness and Infra/performance before M035 is closed.

## Evidence

- `docs/model-architecture/reviews/M035/upstream_algorithm_matrix.md`
- `docs/model-architecture/reviews/M035/test_report.json`
