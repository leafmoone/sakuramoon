# D014: Caption dropout and structured Qwen serializer

Status: CPU code and fixed-protocol validation complete; production use remains blocked by undecided non-global dropout values and Data package review.

## Scope

- Keep the main category skeleton fixed as `nsfw -> character -> copyright -> general -> NL`, shuffle only within each tag category, and select at most one available NL branch uniformly.
- Join tags with exact `, ` and join tags to NL with exact `\n\n`; add no labels.
- Apply fixed `all_condition=0.10`. Require explicit float inputs for nsfw, character, copyright, general, Artist, candidate source, and all five equal NL probabilities; no default or production value is supplied.
- Treat candidate tags as canonical deletion IDs across the four tag categories only.
- Encode `[fixed prefix | main body | fixed suffix | Artist]` once and construct main/Artist indices from segment lengths rather than string search.
- Enforce measured 34-token prefix, 5-token suffix, 512 condition-token limit, eight condition buckets, and full dense lengths 98 through 546.
- Truncate only whole boundaries: remove NL first, then tags from general back toward nsfw; reserve suffix and at least one complete Artist source.

## Implementation

- `src/sakuramoon/data/caption.py`
- `src/sakuramoon/data/serialize.py`
- `tests/unit/data/test_caption.py`
- `tests/contracts/text_protocol/test_caption_serializer.py`

Synthetic test probabilities use only explicit 0.0/1.0 values to force branches. They are test controls, not defaults, recommendations, or production decisions.

## Evidence boundary

Tests cover fixed framing text, tags-only/NL-only/mixed/empty bodies, category order, deterministic internal shuffle, canonical candidate deletion, global/category/Artist/NL dropout branches, thinking-marker rejection, structured indices/masks, null style, complete-boundary truncation, unique Artist preservation, eight dense lengths, and framing-count drift.

The real local Qwen tokenizer measured prefix/suffix as 34/5 tokens with padding ID
248044, and segmented construction matched whole-string encoding across empty, tag,
NL, Artist, and non-English examples. A 100,000-seed scan produced 10,090 fixed
all-condition drops (10.09%).

Padding behavior across a collated batch, real metadata canonical IDs, non-global
dropout distributions, and production truncation distribution remain pending.
Production serialization remains blocked until the user supplies the eleven
unresolved probabilities through strict TOML.
