# T052: Checkpoint-driven evaluation contracts

Status: metric/control-plane remediation, fail-closed checkpoint-driven execution and
the explicit engineering-only bounded mode are complete. Formal checkpoint evaluation
remains blocked on local extractor/preprocess/real-stat identities, explicit
resource/sample bindings and a formal raw/PMA/accepted checkpoint set.

## Allowed paths

- `src/sakuramoon/eval/**`
- `src/sakuramoon/cli/eval.py`
- `src/sakuramoon/cli/__init__.py`
- `src/sakuramoon/config/schema.py`
- `config/eval.toml`
- `config/engineering_eval_s000.toml`
- `config/examples/all_options.example.toml`
- `tests/unit/eval/**`
- `tests/gpu/eval/**`
- `tests/unit/config/conftest.py`
- `tests/unit/config/test_eval_jobs.py`
- `tests/unit/data/test_manifest_cli.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/T052.md`
- `docs/model-architecture/progress/time-log.jsonl`
- `docs/model-architecture/reviews/T052/**`

## Scope

- Derive trend/formal FID and IS jobs only from strict TOML successful-update cadence
  and stage-end settings.
- Bind each job to checkpoint kind/ID/resolved-config identity/checkpoint update,
  prompt/condition/seed/size manifest, successful-update trigger, CFG 2.9,
  Heun-50/99 NFE, sample count,
  extractor/version/preprocess, real-stat provenance, IS splits, GPU index, and explicit
  training-pause policy. The content-addressed job ID must change when any of these
  fields changes.
- Build the prompt plan deterministically from the configured validation selection and
  two local validation tar files; reuse the production typed caption parser/serializer,
  preserve structured tag groups, and reject tags/NL identity drift or insufficient
  cases before CUDA work.
- Compute FID from CPU float64 feature statistics with PSD checks and compute IS from
  deterministic, exactly divisible splits. Immutable scalar artifacts reject
  impossible values before publication (`FID < 0` or `IS < 1`).
- Keep trend/formal/VAE/manual artifacts as distinct kinds, compare raw latest/PMA-10/
  accepted on the same generation/metric protocol, record evaluator GPU/pause cost,
  and forbid automatic release. Resource placement, local manifest path, and trigger
  update remain provenance but do not change checkpoint-comparison semantics.
- Reject checkpoints newer than the evaluation trigger, require raw latest to come from
  the exact trigger update, and permit an accepted comparison checkpoint to be older.
  An accepted release must name its own exact source-PMA path; preflight verifies that
  PMA's ten-source sidecar and strict-JLT lineage/topology independently of the current
  trigger PMA.
- Preserve manual tag control, aesthetic, NL following, composition, detail, and severe
  artifact dimensions instead of treating FID/IS as a release gate.

## Evidence

- Targeted CPU tests cover metric goldens, singular covariance, invalid inputs, strict
  scheduling, deterministic job/prompt identity, immutable artifacts, cost records,
  canonical prompt loading/ordered selection, identity drift, exact IS split
  divisibility, metric-specific scalar lower bounds, protocol-matched three-checkpoint
  comparison, VAE/manual artifact-kind isolation, and checkpoint-bound manual quality
  indexing.
- A real RTX 5090 synthetic plumbing test reran seeded Heun-50/99 NFE bitwise and
  passed the feature/FID/IS path.
- Ruff and strict Pyright pass.

The executable extension verifies canonical local prompt/extractor/preprocess/
real-stat identities, explicit checkpoint role and objective provenance, complete
batch identity, reference Heun-50/99 NFE generation, streaming FID/IS, manual-quality
indexing and atomic complete-tree publication. Missing identities produce structured
blockers and no download or fallback. See `reviews/T052/checkpoint_evaluator_report.md`
and `reviews/T052/checkpoint_evaluator_test_report.json`.

No Inception weights were downloaded, no real feature extractor was substituted, and
no formal FID/IS evaluation ran. Formal `eval.toml` currently reports 13 exact unresolved
bindings covering extractor/version/path, preprocess, real stats, FID/IS sample counts,
IS splits, batch/output reserve and manual sample count. Those are deliberate blockers,
not values to infer.

The real bounded run `evaluation-508d6ccb19d4bbcd362ee637` consumed one RAW checkpoint,
generated two images from the fixed validation tar pairs, published one manual-quality
index and an atomic `COMPLETE` tree, and recorded `automatic_release=false`. It is
permanently classified `synthetic_bounded_engineering_only`; it is not formal FID/IS,
throughput or S001 evidence. The complete evaluator CPU suite passes 144 tests,
including exact CUDA/wall-clock quantization tolerance boundaries without clamping
artifact values and NFSv3-compatible atomic no-clobber publication.
