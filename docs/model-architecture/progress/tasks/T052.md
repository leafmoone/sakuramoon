# T052: Checkpoint-driven evaluation contracts

Status: CPU metric/control-plane code and synthetic real-1GPU deterministic plumbing
complete; formal checkpoint evaluation and independent package review pending.

## Allowed paths

- `src/sakuramoon/eval/**`
- `src/sakuramoon/cli/eval.py`
- `src/sakuramoon/cli/__init__.py`
- `src/sakuramoon/config/schema.py`
- `config/examples/all_options.example.toml`
- `tests/unit/eval/**`
- `tests/gpu/eval/**`
- `tests/unit/config/conftest.py`
- `tests/unit/config/test_eval_jobs.py`
- `tests/unit/data/test_manifest_cli.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/T052.md`
- `docs/model-architecture/progress/time-log.jsonl`
- `docs/model-architecture/reviews/T052/**`

## Scope

- Derive trend/formal FID and IS jobs only from strict TOML successful-update cadence
  and stage-end settings.
- Bind each job to checkpoint kind/ID/resolved hash, prompt/condition/seed/size manifest,
  CFG 2.9, Heun-50/99 NFE, sample count, extractor/version/preprocess, real-stat hash,
  IS splits, GPU index, and explicit training-pause policy.
- Compute FID from CPU float64 feature statistics with PSD checks and compute IS from
  deterministic, exactly divisible splits.
- Keep trend/formal/VAE/manual artifacts as distinct kinds, compare raw latest/PMA-10/
  accepted explicitly, record evaluator GPU/pause cost, and forbid automatic release.
- Preserve manual tag control, aesthetic, NL following, composition, detail, and severe
  artifact dimensions instead of treating FID/IS as a release gate.

## Evidence

- Targeted CPU tests cover metric goldens, singular covariance, invalid inputs, strict
  scheduling, deterministic job/prompt identity, immutable artifacts, cost records,
  three-checkpoint comparison, and manual quality indexing.
- A real RTX 5090 synthetic plumbing test reran seeded Heun-50/99 NFE bitwise and
  passed the feature/FID/IS path.
- Ruff and strict Pyright pass.

No Inception weights were downloaded, no real feature extractor was substituted, and
no 10k/50k evaluation ran. Real checkpoint/Qwen/VAE/DiT sampling remains blocked by
T022/T023 production config and C002/dropout decisions. Formal resource coordination
with four-GPU training and independent AI/Infra review remain pending.
