# A002: 本地模型与参考工程执行硬边界

状态：提交 `477d0a76027dec67dd42c8824108238415d6728f` 的独立 Infra 审查要求继续收窄；第九轮修复及 A002-only 隔离验证完成，等待独立 reviewer 复审与主代理验收

## 需求与依赖

- 依赖 A001 已锁定的 manifest 和 local readiness contract。
- Qwen TE 与 Mage-VAE 已分别位于 `model/qwen_3.5_2B/` 和 `model/vae/`；只允许本地校验与本地加载，禁止下载、补下载、联网替换或 fallback。
- `reference/` 仅供人工理解/对照，可以完全不使用；生产代码、测试、preflight、训练和运行时不得 import、执行或调用其中任何代码。

## 允许修改路径

- `AGENTS.md`
- `docs/model-architecture/current/confirmed-decisions.md`
- `docs/model-architecture/progress/IMPLEMENTATION_ROADMAP.md`
- `docs/model-architecture/progress/asset-policy.md`
- `docs/model-architecture/progress/traceability.toml`
- `tests/contracts/assets/**`
- `src/sakuramoon/assets/__init__.py`
- `src/sakuramoon/assets/inspect.py`
- `tests/unit/assets/conftest.py`
- `tests/unit/assets/test_inspect.py`
- `tools/__init__.py`
- `tools/asset_execution_boundary.py`
- `tests/unit/docs/test_verify_traceability.py`（现行 source revision、requirement count 与新稳定 ID 变化要求治理 fixture 最小同步）
- `docs/model-architecture/progress/tasks/A002.md`
- `docs/model-architecture/progress/time-log.jsonl`
- `docs/model-architecture/reviews/A002/**`

## 动作与验收

- 将两条资产执行硬边界写入现行决定、仓库规则、资产策略与路线图。
- 为新规范分配从未使用的稳定 requirement ID，保留既有 ID，并递增 source/registry revision、更新 SHA-256 链与 fingerprint。
- 独立 scanner 使用受限、fail-closed 的 AST provenance/taint 分析：模块/函数/类作用域、语句顺序、赋值 kill、固定点前向摘要、容器/下标、class attribute、lambda、decorator/default、`match`、`assert`、循环/推导与 `try`/`except*` 都进入分析；无法证明安全的生产高阶调用、跨模块 wrapper 或敏感 callable 传递硬失败。
- 阻止 loader alias、computed `getattr`、`partial`、`.__call__`、star import、未经 runtime readiness capability 证明的根、未知 `**kwargs`、cache/远端路径、`trust_remote_code` 与 capability 反射/构造/子类伪造。模型下载无例外；D010 唯一 dataset transport 为 `src/sakuramoon/data/modelscope.py::ModelScopeDatasetTransport` 的固定标准库 HTTPS 调用图：三个受审计 target factory、验证后的 dataset manifest/shard、默认 TLS context、固定 GET/空 body/header 来源、有限 redirect/header/read/close 操作。SDK/HubApi、任意其他网络库、动态 receiver/method/参数、跨文件或跨类同形调用均硬失败。
- Scanner 包含自身并在读取前拒绝越界、文件或目录 symlink 与身份漂移；合同参数化覆盖 reference import/alias/组合路径/read-then-exec、动态 `setattr`、`subprocess`/`os`/`asyncio`、`sys.path`/`site`、跨模块 wrapper 以及误报正例。
- 参考仓库 Git 身份审计只允许三个只读命令，禁用 local/global/system fsmonitor、hooks、pager、external diff、interactive filter 和 terminal prompt；任何未列出的 Git 命令在 subprocess 前拒绝。
- 测试 Git allowlist 对 `git -C` 的 repo 参数要求来自 synthetic `tmp_path` 根；精确 `_IdentityWeakRegistry.contains` 的 weakref 解引用是唯一生产 callable-parameter 例外，其他类/方法仍拒绝。
- targeted 和全套 CPU 测试、Ruff、strict Pyright 与 traceability checker 全部通过。

## 禁止操作

- 不读取或执行 `reference/` 内容，不读取 `.env`。
- 不下载、哈希、加载或前向任何模型，不使用 GPU。
- 不创建性能占位文件，不创建 commit。

## 自检要求

- AI/模型正确性：确认本地路径与锁定 Qwen/Mage-VAE 身份一致，且静态证据不被误报为真实模型 load/round-trip。
- Infra/性能：确认失败语义不触发网络或大文件 I/O，合同检查只解析仓库 Python 文本，并且不引入训练热路径开销。

## 首轮独立审查收敛

- AI review 要求把测试内启发式检查提升为可复用 scanner，并把每个 `from_pretrained` 来源精确绑定到 A001 已验证的两个本地 asset root；同时移除宽泛 dataset 下载路径例外并覆盖 alias/provenance/taint 绕过。
- Infra review 要求 scanner 自扫描、fail-closed 处理 symlink/越界/读取竞争，并阻止 reference 路径经动态 loader、进程、搜索路径或读取后执行；参考 Git 审计还必须防止 hostile local config 触发 fsmonitor/hook/pager/external command。
- 修复没有读取 `.env`、`model/`、`db/`、`data/` 或 `reference/` 内容，没有联网、下载、模型加载或 GPU 使用。

## 第二轮独立审查收敛

- AI/Infra reviewer 共同指出首轮 scanner 的 provenance/taint 集合跨作用域且不区分语句顺序，重赋值不会 kill；仅靠 annotation/cast、未知 `**kwargs`、ModelScope/computed loader、下载限定路径以及 `.__call__` 仍可绕过。
- Reference taint 已扩展到 shell 字符串、属性、容器、函数返回/参数和 `sys.path` slice，并通过作用域隔离、调用摘要及赋值 kill 消除跨函数同名和重赋值误报。
- Dataset 例外收窄为精确 ModelScope callable、精确生产文件/函数、精确 repo、`load_config()` 锁定 revision provenance 和 `repo_type="dataset"`；Hugging Face 同形调用无例外。
- 测试中的 Git 例外只接受精确 fixture/函数与固定 argv shape；`-c`、alias、未知 argv 和命令后缀均拒绝。Git fixture 改为逐条显式配置，不再依赖 `git -c`。
- `require_verified_selection` 在运行时验证窄 wrapper 接收的 capability 类型并重新检查本地资产不漂移；annotation/cast 不能制造 capability。
- 本轮仍未读取 `.env`、`model/`、`db/`、`data/` 或 `reference/` 内容，没有联网、下载、模型加载或 GPU 使用。

## 第三轮独立审查收敛

- AI reviewer 要求 callable provenance 穿透容器/下标、helper return、class attribute 与动态 `setattr`，静态拒绝 capability 构造/反射/子类伪造，并要求测试 Git `-C` 路径证明来自 synthetic 临时根。A001 最终以 commit `fa435ee72d2d905911ea296c07d1ed3743667a05` 密封运行时 capability：selection 和内层 file 都必须是成功 preflight 工厂登记的精确类型、对象 identity 与签发字段 fingerprint；A002 只负责静态防绕过，不重复归属 A001 代码。
- Infra reviewer 要求从枚举危险 API 收窄为生产受限语言：覆盖 forward helper、class method、lambda、function decorator/default、`match`、`assert`、`try`/`except*`、star import、循环/推导、tuple destructuring、跨模块 wrapper 与敏感 callable 高阶传递；任何不能证明 provenance 的执行路径 fail closed。
- D010 接口最终从候选 SDK facade 收敛为静态标准库 HTTPS transport。旧 `snapshot_download`、SDK/HubApi 与 client-method 例外全部删除；只允许固定 `HTTPSConnection`、`GET`、空 body、默认 TLS context、受审计 header/Range、redirect、response header/read/close 以及三个 target factory 的精确数据流。
- 在 base `fa435ee72d2d905911ea296c07d1ed3743667a05` 上只叠加 `tools/asset_execution_boundary.py` 与 `tests/contracts/assets/test_asset_execution_boundary.py` 的最终隔离候选通过 151 个 boundary contracts、349 个全量测试、28-source scanner（0 violations）、全仓 Ruff、全仓 strict Pyright 与 traceability checker。
- 本轮仍未读取 `.env`、`model/`、`db/`、dataset/cache 或 `reference/` payload，没有模型加载、模型/数据下载或 GPU 使用。隔离候选曾按既有 `uv.lock` 尝试恢复依赖并在 120 秒超时前下载锁定 Python/CUDA 包；最终验证复用主仓同一锁定 `.venv`，不改变训练或资产状态。

## 第四轮 D010 HTTPS 接口收敛

- 精确允许路径只有 `src/sakuramoon/data/modelscope.py`、类 `ModelScopeDatasetTransport` 以及冻结 helper/factory 调用图；同形网络调用移动到其他文件、类或方法会失败。
- `target` provenance 必须来自 `_listing_target`、`_shard_target` 或 `_redirect_target`，公共入口必须先经 `require_verified_dataset_manifest`。首站固定 ModelScope HTTPS 端点；redirect target 只保留受审计 host/port/request-target/auth 形态。
- `headers` 必须来自固定 `_request_headers(target)`：固定 Accept、identity encoding 与 User-Agent；Authorization/Cookie 只在 `target.send_authorization` 时产生。`_open_get` 只允许追加精确 manifest-offset `Range`，任意参数映射、`update` 或重写均拒绝。
- 网络构造只允许 `http.client.HTTPSConnection(host=target.host, port=target.port, timeout=self._policy.connect_timeout_seconds, context=ssl.create_default_context())`；请求只能为固定 `GET`、`target.request_target`、空 body、审计 headers、`encode_chunked=False`。changed receiver、method alias、位置参数、关键字或 `**kwargs` 均拒绝。
- 响应访问只允许冻结位置中的 `Location`/`Content-Length`/`Content-Encoding`/`Content-Range`、`response.read(length)` 与明确 cleanup；SDK、HubApi、alternate HTTP/socket/logging/patching、TLS verification mutation 无例外。

## 第五轮独立审查收敛

- 网络 capability 现在递归穿透容器并采用消费后失效：`target`、`headers`、`connection` 或 `response` 进入 opaque、跨模块或高阶调用会硬失败并清除后续信任。`headers.__setitem__`、`dict.__setitem__`、`__ior__`、`operator.setitem`、mapping alias 与自定义 mutation helper 均不能绕过；`methodcaller`、`attrgetter` 和动态 receiver 也不能间接调用 request/getresponse/close。
- D010 精确调用图锁定冻结实现：`_open_get` 必须以审计的 `try`/redacted-error 形态构造 HTTPS connection；redirect cleanup 必须走 `_close_response`；`_read_response` 只接受按剩余字节数约束的长度；network binding 不接受未验证覆盖。`_ValidatedHttpTarget` 仅允许三个 factory 的直接精确构造，alias/helper 构造失败。
- 动态 `__import__`、动态容器下标、嵌套敏感 callable、高阶 escape、`vars`、`inspect.getattr_static` 与 `from sakuramoon.assets import *` 均 fail closed；`for`/`while` 使用 loop-carried fixed point，终止分支不会把已失效事实重新合并为可信。
- 测试 Git repo provenance 从 synthetic `tmp_path`/holder 沿安全 `/` 组合传播，但拒绝绝对路径、`..` 与 `../../reference`；精确 weakref callable-parameter 例外只覆盖 assets/data 两个 `_IdentityWeakRegistry.contains::reference`，同形其他类继续拒绝。
- 源文件读取改为从已解析仓库根开始逐组件 `dirfd`/`openat`、`O_NOFOLLOW` 锚定，并在同一父目录描述符下完成 leaf identity/read/post-stat；父目录在预检后或 leaf open 前被替换为 symlink 都不能把 scanner 导向仓库外内容。
- 基于 `bcf792ef2968f8fb901bc65b1c289c7b8aa57f17`、仅叠加两个 A002 code/test 路径的隔离候选通过 191 个 boundary contracts、389 个全量测试、28-source scanner（0 violations）、全仓 Ruff、全仓 strict Pyright 与 traceability checker；共享树冻结 D010 兼容扫描为 36 sources、0 violations。
- 本轮仍未读取 `.env`、`model/`、`db/`、dataset/cache 或 `reference/` payload，没有模型加载、模型/数据下载或 GPU 使用；证据只关闭 CPU/static A002 修复，不关闭 D010 真实远端流式、训练或多卡门槛。

## 第六轮独立审查收敛

- `*args` 与非 literal `**kwargs` 展开保留完整递归 fact，覆盖直接/变量 list/tuple、nested unpack、generator、conditional unknown 与相邻 unknown expansion；network、model root、asset/dataset capability 和 sensitive callable 统一进入 opaque/higher-order escape 检查。
- Container join 改为有限且幂等的 shape-unknown widening：长度/键形状变化后不再在 exact/empty 之间振荡，同时把 network/model-root/sensitive/asset/dataset security summary 提升到容器 fact。`set.pop`、`dict.pop`/`popitem` 提取继续保留 loader/capability provenance。
- `break` 环境进入 loop exit，`continue` 环境进入下一轮 back-edge，且当前 block 在 `break`/`continue`/`return`/`raise` 后停止；固定点不再把它们等同为丢弃状态的 return。合同同时断言敏感 loader 穿过 break/continue 且无 nonconvergence。
- Production 禁止无参 `locals`/`vars`/`globals`、`eval`/`exec`/`compile`/动态 code、frame namespace、`operator.attrgetter`/`methodcaller`；D010 `getattr(self, "_open_get")` 等反射 helper 调用和非审计 `object.__getattribute__` 硬失败。仅保留 assets/data capability fingerprint reader 的精确 object access tuple。
- `from ..assets import *` 等相对 assets star import 与绝对 import 同样拒绝。Synthetic Git helper 的第二参数除函数内 root provenance 外，还必须在实际 `make_reference` 调用点证明为无 `..`、非绝对的安全相对 literal；绝对路径与 `../../reference` 拒绝。
- `_read_listing_once.remaining` 由精确 bounded expression 签发 live nonnegative fact，任何重赋值、增量赋值或分支 merge 都 kill；`_read_response(... min(..., remaining))` 只有同时满足冻结 AST 与 live provenance 才允许。
- 基于 `70981e19510c5a6c7d7889d6042b5e8a55887931`、仅叠加两个 A002 code/test 路径的隔离候选通过 232 个 boundary contracts、430 个全量测试、28-source scanner（0 violations）、全仓 Ruff、strict Pyright 与 traceability checker；共享冻结 D010 扫描 36 sources、0 violations、28.165 秒。
- Scanner 仅在 preflight/验证阶段解析源码，不进入 forward/backward/update 训练热路径。本轮仍未读取 `.env`、模型、DB、dataset/cache 或 `reference/` payload，没有模型加载、模型/数据下载、GPU 工作或性能 artifact。

## 第七轮独立审查收敛

- 冻结的 `ModelScopeDatasetTransport` 整类 normalized AST 以 SHA-256 `226d16422aa57d5fcd8c7b1a05ef4cc07f52296f1d21c29e78c40ef23b1567f9` 精确锁定；格式与 source location 不影响摘要，任何结构改写均报告 `dataset_transport_structure_forbidden`。通用 provenance 规则仍作为第二层，不能用结构 pin 替代。
- Closure/generator/default/signature/frame namespace、`type.__getattribute__`、`functools.reduce`、`inspect.getattr_static`、参数化 helper/lambda loader selection 与非 literal `getattr` 均 fail closed。现有两处 fingerprint `vars` 和 bindings 动态字段比较只通过精确 path/function/receiver/attribute tuple 白名单。
- Synthetic Git `make_reference` 获得不可逃逸 capability；传入 `invoke` 等高阶 helper 或从 fixture `globals()` 恢复均拒绝。直接调用仍要求安全相对 literal，不能用 wrapper 绕过 `../../reference` 检查。
- Listing read 的 `remaining` 不再仅凭文本表达式签发：`payload` 必须由空 `bytearray()` 初始化或在确定终止的超限 guard 后重新获得 upper-bound fact；`extend` 等 mutation 会立即 kill。因而未证明长度、mutation 后无 guard 或负长度 `read(-1)` 均失败。
- 基于 `9081104ba0a87ce72efbeac3125f975b7e3fb71d`、仅叠加两个 A002 code/test 路径的隔离候选通过 271 个 boundary contracts、469 个全量测试、28-source scanner（0 violations）、全仓 Ruff、strict Pyright 与 traceability checker；共享冻结 D010 扫描 36 sources、0 violations、30.096 秒。
- Scanner 仍只在 preflight/验证阶段解析源码，不进入训练热路径。本轮没有读取 `.env`、模型、DB、dataset/cache 或 `reference/` payload，没有联网、模型/数据下载、GPU 工作或性能 artifact；状态保持等待独立复审。

## 第八轮独立审查收敛

- Callable annotation/default storage 纳入统一 security-capability 定义时检查：model root、sensitive loader、asset selection 或 D010 target/header/connection 不能进入 positional default、kw-only default 或动态 annotation。`__annotations__`、`annotation`、`return_annotation`、`typing.get_type_hints` 与 literal `getattr(..., "__defaults__")` 均属于禁止的 execution namespace。
- `functools.partial`/`partialmethod` 不得绑定 `getattr`/`vars`/`__getattribute__` 等反射 callable；`sys.modules["builtins"]`、`__builtins__` 与 `inspect.getmembers`/`getmembers_static` 不能恢复动态 builtins 或 loader。Synthetic Git `make_reference` 同样不能经 default storage 或 partial 绕过安全相对路径检查。
- A002 合同现在内嵌压缩的纯 UTF-8 frozen D010 fixture：包含完整 `ModelScopeDatasetTransport` 类、最小 module prelude 与 inert helper summaries；仅解压、`ast.parse` 和扫描，不 import/execute。Fixture payload SHA-256、整类 normalized-AST digest 与 scanner pin 均被固定，clean tree 可独立证明 pin 成功路径且 fixture 扫描零违规。
- 共享 D010 存在时另行断言其完整 class normalized AST 与 tracked fixture 完全一致；clean A002 candidate 不含 D010 source 时只跳过该共享一致性断言，不影响 fixture/pin 的可重放正证据，避免 A002 与 D010 提交循环依赖。
- 基于 `cadd87391ef07ed7edb2932e8e2b339b268838ff`、仅叠加两个 A002 code/test 路径的隔离候选通过 307 个 boundary contracts、505 个全量测试（各 1 个共享 D010 一致性检查按设计 skipped）、28-source scanner（0 violations）、全仓 Ruff、strict Pyright 与 traceability checker；共享树执行 308 个 boundary contracts并扫描 36 sources/0 violations。
- Scanner 仍只在 preflight/验证阶段解析源码，不进入训练热路径。本轮没有读取 `.env`、模型、DB、dataset/cache 或 `reference/` payload，没有联网、模型/数据下载、GPU 工作或性能 artifact；状态保持等待独立复审。

## 第九轮独立审查收敛

- 反射 callable 使用统一 security-sensitive 分类：`getattr`、`vars`、`object/type.__getattribute__`、namespace inspection 与静态/高阶 member selector 在事实传播中不再退化为普通 callable。所有直接 special-call 判定先移除任意重复尾随 `.__call__`，所以 `getattr.__call__`、重复链及 `builtins.getattr.__call__` 与直接 `getattr` 保持同一 model-loader provenance。
- `partial`/`partialmethod` 绑定反射 callable 直接失败；反射 callable 进入 `operator.call`、`map`/`next`、本地 helper 或未知跨模块 adapter 时触发统一 sensitive/security capability escape。实现依赖 security fact 的递归传播与 fail-closed higher-order gate，不为每个 adapter 单独建立可放行路径。
- Production 的 `.__dict__` 被视为 execution namespace，阻止 `sys.__dict__["modules"]["builtins"]`、`builtins.__dict__`、`importlib.__dict__` 与敏感库 module/class namespace 恢复调用能力；普通显式 local member 的 literal `getattr.__call__` 正例仍可通过。
- 回归同时覆盖现有 model loader/root/`VerifiedAssetSelection`、synthetic Git helper、D010 network capability 与 frozen transport fixture/pin 邻接。基于 `477d0a76027dec67dd42c8824108238415d6728f`、仅叠加两个 A002 code/test 路径的隔离候选通过 328 个 boundary contracts（1 个共享 D010 一致性检查按设计 skipped）、526 个全量测试（同一 skip）、28-source scanner 0 violations、全仓 Ruff、strict Pyright 与 traceability checker；共享树通过 329 个 boundary contracts且扫描 36 sources/0 violations。
- Clean scanner 为 20.67 秒，低于本轮受审提交的 21.729 秒；共享 scanner 为 30.97 秒，低于第八轮 31.595 秒，未观察到性能回退。Scanner 仍仅在 preflight/验证执行；没有读取 `.env`、模型、DB、dataset/cache 或 `reference/` payload，没有联网、模型/数据下载、GPU 工作或性能 artifact。
