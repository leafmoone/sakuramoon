# D019: Candidate deletion ID validation

Status: implementation and targeted CPU validation complete; Data package rereview
pending. Implementation commit reference: `task:D019`.

## Dependencies

- D014 owns caption dropout and canonical candidate deletion semantics.
- `C03-011` requires candidate tags to act only as canonical deletion matches across
  the four tag categories.

## Allowed paths

- `src/sakuramoon/data/caption.py`
- `tests/unit/data/test_caption.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/D019.md`
- `docs/model-architecture/reviews/D019/**`
- `docs/model-architecture/reviews/DATA/**`

## Scope

- Validate `CaptionFields.candidate_tags` before any deterministic dropout decision.
- Require an exact `frozenset` of non-empty, trim-stable strings so malformed IDs
  cannot silently evade exact canonical membership deletion.
- Reject mutable sets and non-string members instead of coercing them.

## Non-goals

- No change to global/component dropout probabilities, RNG domains, category order,
  deletion scope, serializer behavior, or component hit telemetry.
- No production 100,000-sample distribution run, GPU, DDP/NCCL, multi-GPU, long run,
  or formal stage evidence is claimed.

## Evidence

- `docs/model-architecture/reviews/D019/test_report.json`
