# G001: Forward governance evidence simplification

Status: complete

## Dependencies and scope

- Starts after the D015 atomic commit and remains the only repository-writing task.
- Changes governance and trace validation only; no runtime, training, asset, dataset,
  checkpoint, DDP, NCCL, or long-run behavior is executed.
- Preserves every existing requirement ID, fingerprint, evidence file, and commit
  reference. The archive remains read-only.

## Allowed paths

- `AGENTS.md`
- `README.md`
- `docs/model-architecture/current/confirmed-decisions.md`
- `docs/model-architecture/current/open-items.md`
- `docs/model-architecture/progress/IMPLEMENTATION_ROADMAP.md`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/G001.md`
- `docs/model-architecture/reviews/G001/test_report.json`
- `tools/verify_traceability.py`
- `tests/unit/docs/test_verify_traceability.py`

## Implementation paths

- Governance policy: `AGENTS.md`, `README.md`, current review clauses, and roadmap.
- Machine enforcement: `tools/verify_traceability.py` and `traceability.toml`.
- Contracts: `tests/unit/docs/test_verify_traceability.py`.

## Tests and status gates

- Wording-only current-source changes retain historical fingerprints and pass after a
  normal source revision/SHA changelog update.
- Requirement additions/deletions, source/heading/node-kind drift, ID reorder/reuse,
  and historical fingerprint rewrites continue to fail.
- Package review paths are required for the declared low/medium-risk packages;
  high-risk tasks and formal stage tasks require task-specific AI/Infra files.
- Existing timing, review, test, evidence, and commit fixtures remain tracked and
  unchanged. G001 creates no independent timing or review artifact.

Hardware: CPU.

## Results

- Trace verifier contracts: 40 passed.
- CPU repository regression: 561 passed with the one real CUDA allocator OOM test
  explicitly deselected.
- Full unfiltered regression: 561 passed and the same CUDA allocator OOM test failed;
  its isolated rerun also failed while `nvidia-smi` reported a driver/library version
  mismatch. This environmental GPU failure does not close or alter any GPU gate.
- Full-repository Ruff, strict Pyright, `uv lock --check`, live traceability, diff
  hygiene, and the 221-entry historical identity/evidence comparison passed.
