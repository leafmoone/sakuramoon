# T022: Main text conditioner

Status: CPU/1GPU implementation and hot-path sync remediation complete; the independent
Encoders package rereview returned PASS for the implemented scope.

## Scope

- Gather only structured main-token indices from the seven frozen Qwen states.
- Apply seven independent FP32-accumulating RMSNorms, one shared input projection, and token-dependent 8-group layer mixing.
- Keep the deepest-layer anchor, one non-causal attention-only QK-normalized block, padding isolation, and final projection.
- Sanitize every masked index before gather while retaining hard failure for invalid
  active indices.
- The production factory fixes only approved dimensions and precision. It requires the
  still-unlocked MHA/mix-gate/LayerScale/bias choices explicitly and invents no default.

## Evidence

- `tests/unit/conditioning/test_text_mixer.py`: 7 passed in 3.23 seconds.
- Tests cover gather isolation, uniform initial layer weights, frozen-input gradient boundary, bidirectional dependence, and padding isolation.
- New contracts cover masked `int64` extremes, active out-of-range hard failures, and
  the production factory's locked-versus-explicit parameter boundary.
- Package Ruff and strict Pyright passed.
- RTX 5090 BF16 forward/backward produced `[2,32,2560]`, kept padding zero,
  detached Qwen inputs, and produced finite gradients for all reached adapter parameters.
- With explicit smoke-only constructor choices, warm forward was 2.25-2.58 ms,
  backward was 2.31-2.66 ms, and peak allocated memory was 114.8 MiB.
- Fresh targeted RTX 5090/driver 580.105.08 evidence: production-shape BF16
  forward/backward returned finite `[2,32,2560]`, kept padded outputs zero, and rejected
  masked extreme indices before gather; 1 passed in 2.60 seconds.
- Traceability contracts: 40 passed in 20.24 seconds; direct verification passed.
- Independent package review found that CUDA boolean compression plus Python `min/max`
  synchronized the training hot path. Remediation moved structured-index value
  validation to CPU collate, uses fixed-shape device predicates, and retains a
  device-side asynchronous hard assertion for direct CUDA callers.
- Remediation contracts: 19 targeted CPU tests passed. The production-shape RTX 5090
  forward/backward passed with CUDA synchronization debug mode set to `error`; no
  supported host-synchronization detector fired on the valid forward.

The smoke-only head/init choices are not recorded as production defaults. They remain
explicit constructor inputs until the corresponding configuration decision is locked.
Encoders/Conditioning package rereview remains pending. No multi-GPU, DDP/NCCL, or
training long run was executed.
