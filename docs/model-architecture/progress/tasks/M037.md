# M037: Noise observation boundary revision

Status: implementation and targeted CPU/one-GPU validation passed; independent Dense
remediation review pending.

## Dependencies

- M033 strict JLT remediation commit
  `7a36605fb17235580262b76cab07181076aad459` is complete.
- M035 commit `cf6c4ce5c96ceb409d6fa04b833673c50a17171c` records the
  locked JLT comparison and qualifies the existing component CUDA smoke.
- R001 locks JLT to commit
  `aca236efa97aab3b7d865fd3d99a270431cf6ae5`.
- The user revised the SakuraMoon observation-only boundary from `t=0.5` to
  `t=0.95`. This does not revise the strict JLT objective.

## Allowed paths

- `src/sakuramoon/objective/flow.py`
- `src/sakuramoon/config/schema.py`
- `config/examples/all_options.example.toml`
- `tests/unit/objective/test_flow.py`
- `tests/gpu/objective/test_flow_sampling.py`
- `tests/unit/config/test_schema.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/current/confirmed-decisions.md`
- `docs/model-architecture/current/open-items.md`
- `docs/model-architecture/current/observability-and-evaluation.md`
- `docs/model-architecture/progress/IMPLEMENTATION_ROADMAP.md`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/M037.md`
- `docs/model-architecture/reviews/M037/upstream_algorithm_matrix.md`
- `docs/model-architecture/reviews/M037/test_report.json`

## Scope

- Require `[logging].noise_observation_boundary=0.95` as an explicit fixed TOML
  value and reject missing, wrongly typed, or drifting values.
- Require the resolved value at the objective call site and aggregate high-noise
  sums/counts for `t<0.95` and low-noise sums/counts for `t>=0.95`.
- Preserve the full-batch strict JLT loss, prediction parameterization, shared
  clamp, inverse-square weighting, reduction order, dtype, CFG, and sampler.
- Preserve the historical `t=0.5` decision and evidence records, while appending
  a current revision and stable M037 trace mappings.

## Non-goals

- No loss reweighting, training distribution change, timestep resampling, sampler
  profile/config change, CFG change, or runtime W&B implementation.
- No rewrite of historical M033/M035 review evidence or fingerprints.
- No claim that JLT upstream defines high/low observation buckets or uses `0.95`
  as a loss threshold.
- No full data/Qwen/VAE/DiT/loss/checkpoint training integration; T050 retains
  that one-to-ten-step engineering smoke.
- No DDP, NCCL, multi-GPU, long-run, or formal-stage claim.

## Validation

- Run targeted objective/config CPU contracts and the one-GPU objective smoke.
- Run objective/config regressions, trace contracts, the live trace verifier,
  Ruff, strict Pyright, lock validation, and diff hygiene.
- Record an AI/model self-check that the loss tensor and gradient path are
  unchanged, and an Infra/performance self-check that the change adds only one
  vectorized comparison/mask for already-required telemetry.
- Obtain an independent Dense remediation review before representing M037 as
  review-complete.

## Evidence

- `docs/model-architecture/reviews/M037/upstream_algorithm_matrix.md`
- `docs/model-architecture/reviews/M037/test_report.json`
