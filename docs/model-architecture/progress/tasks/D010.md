# D010: ModelScope dataset manifest and shard streaming

Status: Data package review finding remediated and accepted by the main agent

## Allowed paths

- `src/sakuramoon/data/manifest.py`
- `src/sakuramoon/data/modelscope.py`
- `src/sakuramoon/cli/manifest.py`
- `tests/unit/data/test_dataset_manifest.py`
- `tests/unit/data/test_modelscope.py`
- `tests/unit/data/test_manifest_cli.py`
- `tests/contracts/data/test_manifest_cli_contract.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/D010.md`
- `docs/model-architecture/progress/time-log.jsonl`
- `docs/model-architecture/reviews/D010/**`
- `docs/model-architecture/reviews/DATA/**`

## Scope

- Fix the remote source to `leafmoone/webdataset_danbooru` and a 40-character commit revision.
- Describe every WebDataset shard with path, release, byte count, SHA-256, and sample count.
- Validate the remote tar inventory against the manifest.
- Download only a requested shard, stream it into `.partial`, hash while writing, then publish it with `os.replace`.
- Read `MODELSCOPE_API_TOKEN` only through the configured environment-variable name.

D010 does not pre-download the dataset. D011-D015 own metadata validation, cache coordination, image/caption processing, and the WebDataset training pipeline.

## Implementation

- `src/sakuramoon/data/manifest.py`: strict canonical manifest and config binding.
- `src/sakuramoon/data/modelscope.py`: small HTTPS listing/download client and verified shard publication.
- `src/sakuramoon/cli/manifest.py`: local or remote manifest verification.
- `src/sakuramoon/config/schema.py`: explicit source and transport settings.

There is no capability registry, inode/dirfd protocol, TOCTOU layer, resume protocol, reference-code call, or local model hash check.

## Validation

- D010 manifest/transport/CLI contracts: 41 passed in 1.71 seconds.
- Full CPU suite: 349 passed in 31.01 seconds. Ruff, strict Pyright, lock check, and traceability verification passed.
- Tests use synthetic HTTP responses and tiny shard payloads. A live ModelScope revision remains runtime configuration, not a guessed value.
- Data package review found that set equality could hide a duplicate remote shard record. Validation now requires the remote tar entry count and exact `(path, bytes, SHA-256)` set to match the immutable manifest; an identical duplicate is a hard failure.
- Production enumeration, source access evidence, and a real network smoke remain pending until an immutable revision and runtime credential are supplied. No network call was made for this remediation.

GPU: not required.
