# D024: Process-isolated dataset supply service

Status: CPU implementation, real multiprocessing fault validation, and bounded RTX
5090 consumer smoke passed. Independent DATA package review remains pending. The
production cold-cache/network/NVMe performance gate remains blocked and is not inferred
from local fixtures.

## Allowed paths

- `config/examples/all_options.example.toml`
- `src/sakuramoon/config/schema.py`
- `src/sakuramoon/cli/data_service.py`
- `src/sakuramoon/data/__init__.py`
- `src/sakuramoon/data/cache.py`
- `src/sakuramoon/data/client.py`
- `src/sakuramoon/data/collate.py`
- `src/sakuramoon/data/pipeline.py`
- `src/sakuramoon/data/service.py`
- `src/sakuramoon/data/service_protocol.py`
- `src/sakuramoon/data/state.py`
- `tests/unit/config/**`
- `tests/unit/data/test_cache.py`
- `tests/unit/data/test_d024_collate.py`
- `tests/unit/data/test_d024_service.py`
- `tests/unit/data/test_manifest_cli.py`
- `tests/fault_injection/test_d024_service_faults.py`
- `tests/gpu/data/test_d024_service_consumer.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/current/confirmed-decisions.md`
- `docs/model-architecture/current/open-items.md`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/D024.md`
- `docs/model-architecture/reviews/D024/**`

`docs/model-architecture/progress/IMPLEMENTATION_ROADMAP.md` contains a separate user
worktree change and is deliberately excluded from the D024 commit.

## Service contract

- One independently launched AF_UNIX service process exclusively owns ModelScope
  transport, download partials, byte/SHA verification, atomic shard publication,
  cache reservations/LRU/eviction, tar order, persistent status, and replay accounting.
- The service persists one full-manifest random `mainset` containing a random
  `mainset_id`, shuffle identity, exact ordinal permutation, per-row status, exact
  worker count, and replay counters. It rejects manifest, topology, permutation,
  ordinal, status-order, and schema drift.
- Trainer lifecycle inputs cannot affect data order. IPC identity contains only the
  immutable manifest digest and exact worker count; stage, pass, seed, run,
  checkpoint parent, cursor, snapshot, and resume data position are absent.
- The service re-prepares every recovered active row before scheduling pending
  lookahead. New activation is persisted before lease publication, and every active,
  ready, and in-flight path remains protected from eviction.
- Download concurrency, verified lookahead, lease/ACK capacity, per-worker input,
  ready batches, and completion output are all explicitly bounded. Deprecated
  `range_workers` is rejected rather than accepted without effect.
- Only normal DataLoader exhaustion produces an ACK. Worker exit, parent close,
  service kill, client disconnect, missing ACK, or publication failure leaves the row
  active for whole-shard replay. The final ACK rotates directly to a newly persisted
  full random mainset only when no outstanding lease, ready shard, or fetch remains.

## Preserved contracts

D024 keeps D021 trusted `ShardRecord`, explicit metadata mapping/adapter, validation
exclusion, caption/image/collate contracts, and D022/D023 whole-shard at-least-once
semantics. The trainer-side client and worker import path does not import transport,
cache, eviction, partial cleanup, or legacy shard-state ownership. The D022 singleton
constructor remains compatible with the historical checkpoint caller while persistent
schema-v3 behavior is unchanged.

## Evidence boundary

Targeted service/mainset/fault/two-worker contracts, adjacent data/config/state/trace
regressions, static checks, and one bounded real-CUDA consumer smoke passed. No `.env`
was read. No production manifest, live ModelScope cold-cache run, two-hour sweep,
formal NVMe benchmark, long training, DDP/NCCL, multi-GPU run, 1,000-step canary, or
formal stage was run. Production `>=12 samples/s`, ready wait `<2%`, RSS/swap, quota,
and fully-cached control comparisons remain blocked/pending.
