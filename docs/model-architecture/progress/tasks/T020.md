# T020: Mage-VAE wrapper

Status: CPU/1GPU implementation and governed Mage upstream provenance remediation
complete; the independent Encoders package rereview returned PASS for the implemented
scope. Production metric implementation, 2,000-image acceptance, and latent statistics
remain pending.

## Allowed paths

- `src/sakuramoon/encoders/mage_vae.py`
- `src/sakuramoon/eval/__init__.py`
- `src/sakuramoon/eval/vae_reconstruction.py`
- `tests/unit/encoders/test_mage_vae.py`
- `tests/unit/eval/test_vae_reconstruction.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/T020.md`
- `docs/model-architecture/progress/time-log.jsonl`
- `docs/model-architecture/reviews/T020/**`

## Scope

- Implement the Microsoft Mage checkpoint structure needed by the prepared local safetensors file.
- Load only `model/vae/diffusion_pytorch_model.safetensors`; never download or fall back.
- Freeze the model in eval/inference mode and always return posterior mean latents shaped `[B,128,H/16,W/16]`.
- Preserve the decoder round-trip path without extra patchification.
- Bind the fixed 2,000-image cohort to an immutable canonical manifest with exact IDs,
  cohort labels, dimensions, and 1,500 base-512/500 extended resolution classes before
  any VAE forward. Derive the recorded SHA-256 from that manifest instead of accepting
  a caller-supplied digest.
- Aggregate exactly 1,600 stratified and 400 risk observations with finite metrics,
  manual labels, and all locked quality gates.
- Execute every batch through the frozen VAE encode/decode path and a required external
  LPIPS/SSIM engine. The engine has no fallback or code default; its implementation,
  version, weights, and parameters are explicit artifact identity fields.
- Source metric identity only from the injected metric engine and publish all 2,000
  observations plus the aggregate report as canonical JSON using a temporary file,
  file fsync, and `os.replace`.
- Govern the upstream implementation independently at Microsoft Mage commit
  `8c94a0ac905167f40b05b09332b78752b7f9fbef`, including the Git tree, source digest,
  MIT license digest, and a static contract comparison. This provenance is not the
  R001 historical reference snapshot or a local model identity manifest.

## Evidence

- Strict local state loading passed for 321 encoder and 365 decoder state entries.
- `tests/unit/encoders/test_mage_vae.py`: 2 passed.
- Package Ruff and strict Pyright passed.
- Reconstruction evaluator contracts cover cohort identity, metric/label validity,
  nearest-rank P95, and each independent acceptance threshold.
- Executor contracts cover real tensor round trips, inference mode, metric vector
  shape/finite checks, exact canonical member/plan preflight before model work, a
  manifest-derived hash, engine-owned metric identity, exact cohort closure, and report
  recomputation.
- Artifact contracts cover canonical identity, replacement of a prior reproducible
  report, file fsync, atomic `os.replace`, temporary cleanup, and preservation of the
  prior report when replacement fails.
- RTX 5090 512x512 BF16: latent `[1,128,32,32]`, reconstruction `[1,3,512,512]`, finite outputs, 375.6 MiB peak allocated memory.
- After one warmup, encode was 9.40/8.71/8.50 ms and decode was 11.50/10.95/10.75 ms.

Targeted CPU contracts: 34 passed in 2.87 seconds. The prior full CPU suite evidence is
retained: 507 passed in 36.97 seconds with 24 warnings. Traceability contracts: 40
passed in 16.62 seconds. Ruff, strict Pyright, and the direct trace verifier passed.

The production LPIPS/SSIM engine identity, 50k-100k latent statistics, and actual
2,000-image LPIPS/SSIM/manual quality gate remain pending and are not inferred from the
synthetic injected metric engine or the GPU smoke. Encoders/Conditioning package review
remains pending until T020-T024 are frozen. No multi-GPU, DDP/NCCL, 2,000-image quality
run, or training long run was executed.

The governed lock now pins the Mage repository, commit, Git tree, upstream source
SHA-256, and MIT license SHA-256. The static matrix compares checkpoint prefixes,
posterior mean selection, zero-timestep encode/decode, replicate-padded attention, and
the no-patch-packing geometry against SakuraMoon. No `reference/` code or upstream code
is imported or executed, R001 evidence is unchanged, and local model files are not
hashed or assigned a new identity.

The governed-lock contract and existing reconstruction evaluator suite pass 35 CPU
tests. Ruff, strict Pyright, and direct trace verification pass. Full trace contract
results are recorded in the separate upstream provenance report after final validation.
