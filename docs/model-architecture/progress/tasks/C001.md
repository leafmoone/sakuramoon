# C001: Strict TOML schema, merge, and redaction

Status: Foundation independent review evidence issue remediated and accepted by the main agent

## Requirements and dependencies

- Depends on the committed R002 environment lock and D001 traceability registry.
- Implements the C001 contract from the roadmap, the configuration requirements in `OBS-*`, and the fail-closed portion of the open dropout/preflight items.
- Does not select any unresolved dropout probability and does not create production stage overlays.

## Allowed paths

- `src/sakuramoon/__init__.py`
- `src/sakuramoon/config/**`
- `tests/unit/config/**`
- `tests/contracts/config/**`
- `tests/unit/docs/test_verify_traceability.py`
- `config/examples/all_options.example.toml`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/C001.md`
- `docs/model-architecture/progress/time-log.jsonl`
- `docs/model-architecture/reviews/C001/**`
- `docs/model-architecture/reviews/FOUNDATION/**`

## Actions and acceptance

- Use frozen Pydantic v2 strict models with `extra="forbid"`; all training-semantic fields are required in code.
- Load TOML through deterministic, root-confined `extends`; reject traversal, symlinks, duplicate includes, cycles, and table/scalar merge conflicts.
- Reject unknown, missing, wrong-type, out-of-range, fixed-architecture, NL-dropout mismatch, and invalid stage-transition values.
- Keep only secret environment-variable names in configuration. Validate named variables without reading `.env`; never place their values in resolved TOML, hashes, errors, or artifacts.
- Serialize a deterministic resolved TOML and SHA-256. The options example is documentation-only and must fail runtime validation while dropout decisions remain unresolved.
- Add targeted unit and contract tests, implementation summary, timing, and test evidence. Do not create performance placeholder files.

Hardware level: CPU.

## Implementation result

- Added frozen Pydantic v2 strict models for the current Foundation runtime surface. All 234 runtime config keys are explicit, every model rejects extras, all present semantic fields are required in code, and every TOML float must use float syntax and be finite; target-machine stage budget fields are explicitly deferred to S000.
- Added deterministic recursive table merge and whole-value scalar/array replacement with root confinement, lexical symlink checks, cycle/duplicate rejection, and safe errors.
- Added environment-name-only credential handling, transient masked secret resolution, deterministic redacted TOML, exact resolved SHA-256, and an atomic writer that validates ancestors before creating directories.
- Kept `all_condition=0.10` fixed. The other 11 probabilities remain string sentinels in the documentation example, which runtime rejects before schema validation. Numeric test values are labeled synthetic and are not defaults or recommendations.
- Review-remediated targeted configuration tests, the complete CPU suite, Ruff, strict Pyright, lock check, and live D001 checker pass. No GPU was used and no performance placeholder was created.
- Main acceptance removed the public secret-validation bypass and tightened boundary-aware redaction so credential fields are masked while fixed local paths and non-secret dataset provenance remain auditable.
- The selected stage must be enabled and FID/IS acceptance sample counts remain required but benchmark-configurable. Only the remote dataset revision requires a 40-character lowercase commit; Qwen and VAE have fixed local paths and no repo, revision, tokenizer hash, manifest, or identity fields.

## Review result

- Foundation AI review found that the prior task/report described withdrawn model identity fields; Foundation Infra review passed the implementation but blocked evidence closure on the same issue. The stale claims were removed without changing the already-correct schema.
- Direct independent re-review startup did not return a valid task name. Task-specific review files preserve the original independent finding and identify the final conclusion as main-agent remediation acceptance.
