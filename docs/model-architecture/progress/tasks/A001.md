# A001: 本地资产审计方案撤销

状态：重型方案保持撤销；Foundation 独立审查发现的最小组件边界已修复并通过主代理验收

## 允许修改路径

- `.gitignore`
- `src/sakuramoon/assets/__init__.py`
- `src/sakuramoon/encoders/mage_vae.py`
- `src/sakuramoon/encoders/qwen.py`
- `tests/unit/assets/test_local_models.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/progress/IMPLEMENTATION_ROADMAP.md`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/A001.md`
- `docs/model-architecture/progress/time-log.jsonl`
- `docs/model-architecture/reviews/A001/**`
- `docs/model-architecture/reviews/FOUNDATION/**`

## 变更决定

用户已确认 Qwen TE 与 Mage-VAE 分别准备在 `model/qwen_3.5_2B/` 和 `model/vae/`。本项目是个人兴趣项目，不再为这些已准备的本地资产维护逐文件 bytes/SHA-256、manifest 自哈希、repo/revision/license、DB schema hash、reference Git 身份或 capability 签发体系。

原 A001 的以下范围全部撤销：

- `assets/manifest.toml` 及其 schema、hash report、license/source report。
- 对本地模型、DB 和参考工程做逐文件 bytes/SHA-256 审计。
- asset inspection CLI、verified identity/capability、发行登记、TOCTOU/hostile-local-environment 防护。
- 为上述机制维护的专属实现、测试和多轮审查证据。

## 保留的最小边界

- Qwen 只从固定本地目录 `model/qwen_3.5_2B/` 加载。
- Mage-VAE 只从固定本地目录 `model/vae/` 加载。
- 实际加载前检查目录和加载所必需的文件是否存在；缺失时明确失败。
- 禁止自动下载、缺失补下载、联网替换和任何 fallback。
- 模型、DB、dataset/cache、`reference/` 与训练 artifact 继续保持 Git 忽略。
- `reference/` 只供人工理解和对照，生产代码、测试、preflight、训练和运行时不得 import、执行或调用其中代码。

Qwen/Mage-VAE 的真实配置、接口、forward 和质量验证分别归 `T021`/`T020`；DB/metadata 只在数据任务实际需要时按 schema 读取。D010 对远端 WebDataset shard 的 bytes/SHA-256 校验属于网络传输完整性，不受本次撤销影响。

## 结果

A001 不再要求独立资产 manifest、哈希扫描、CLI、capability 或重型审查轮次，也不再作为 `D010`、`T020`、`T021` 的前置阻塞项。

Foundation 独立审查发现聚合 `require_local_models()` 使两个组件互相依赖。修复增加 `require_local_qwen()` 与 `require_local_vae()`，实际 loader 只检查自身必需文件；聚合函数仅保留给显式全模型 preflight。仅准备单个组件文件的 unit 合同证明另一组件缺失不会阻塞它。

`.gitignore` 的旧 manifest 注释已改为直接禁止追踪本地权重与数据库。未恢复 manifest、bytes/SHA-256、repo/revision/license、capability、TOCTOU、下载或 fallback。直接独立复审启动未返回有效任务名，逐任务报告明确区分初次独立发现与主代理修复验收。
