# A001: 本地 Qwen/VAE/DB/参考工程资产锁

状态：第二轮审查修复完成；等待独立 AI/Infra 复审与主代理验收

## 需求与依赖

- 依赖 R002 的 uv/Python 3.12 锁；资产哈希与只读配置检查可提前并行。
- 依据 `C02`、`C03`、`C11`、路线图 `A001` 和 R001 资产边界。

## 允许修改路径

- `assets/manifest.toml`
- `src/sakuramoon/assets/**`
- `src/sakuramoon/cli/**`
- `tests/unit/assets/**`
- `tests/contracts/assets/**`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/A001.md`
- `docs/model-architecture/progress/time-log.jsonl`
- `docs/model-architecture/reviews/A001/**`
- `docs/model-architecture/reviews/FOUNDATION/**`

## 动作与验收

- 记录本地 Qwen、Mage-VAE、DB 与三个参考工程的 repo/revision/path/bytes/SHA-256/config 摘要/tokenizer hash/许可证。
- 验证 Qwen config 为 24L/2048 hidden；验证 Mage-VAE config/权重声明 128 latent channels、downsample 16 与 posterior mean 接口要求。
- 任一文件缺失或 hash 漂移必须在模型加载前失败；模型、DB 和 reference 目录不得进入 Git。
- 不复制来源不明代码；本任务只锁资产，不把静态 config 检查冒充真实模型 load/round-trip。

## 并行边界

- R002 期间只允许只读哈希、配置与许可证预计算，写入独立 A001 证据文件；生产模块和测试等 R002 提交后再实现。
- 不读取 `.env`，不下载数据集，不执行 GPU，不创建 commit。

硬件等级：CPU；模型加载留到 T020/T021。

## 实现结果

- 新增严格的 `assets/manifest.toml` schema、根目录约束、逐文件 bytes/SHA-256 检查、Qwen/Mage-VAE config 契约检查和三个参考仓库的 commit/origin/license 检查。
- Qwen 已按用户确认的现成资产登记为 ready；ModelScope 固定 revision `7a387e3c9c23e913bbbe0b20fcbd1c5b38c0b96f`，并锁定权重、tokenizer、config 与 license 的上游 SHA-256。未重新读取模型权重。
- Mage-VAE 已按用户确认的现成资产登记为 ready；官方来源锁定到 `microsoft/Mage-Flow` revision `faca09c18c1c19458e7fbc3f7bce6f7a7d4d01a9`、MIT 和逐文件 SHA-256。posterior mean 的真实接口/round-trip 留到 T020。
- 新增单一 root-confined runtime readiness 入口；同一次 manifest 字节读取同时用于严格解析、digest、runtime 文件检查和 config 精确绑定。外部/symlink manifest、模型漂移或检查后的 manifest/文件身份漂移均硬失败。
- 用户确认训练 caption 已随 WebDataset 以 JSON 准备完成，三份 DB/Parquet 均为非运行时可选参考资产。文件已顺序计算 SHA-256，并仅从 DuckDB catalog/Parquet footer 提取 schema hash；未读取数据行。
- `dan_5_9.db` 记录为用户自有、Danbooru 派生的本地快照；两份 Minthy Parquet 锁定公开 repo/revision/license。三者都不参与 runtime preflight。
- 可选 DB 审计要求显式选择 asset ID，并在数据库库打开文件前依次验证 missing/bytes/SHA-256；成功后只交付带重验证契约的 verified identity。
- HDM、JLT、krea-2 的 origin、HEAD、tracked worktree 和 license 检查已拆为独立开发审计，不再阻塞生产 runtime；origin/worktree 诊断不回显潜在敏感值。未复制参考代码。
- bytes 已漂移时立即失败并跳过 payload hash，避免对已经失败的多 GiB 文件继续顺序读取。
- 资产诊断 CLI 现在稳定区分 `0=检查通过`、`1=资产不合约报告`、`2=调用/manifest/preflight 错误`；所有失败均输出单行脱敏 JSON，不再由 `AssetPreflightError` 或底层 I/O 异常泄漏 traceback、绝对路径或原始 errno。
- argparse 的 invalid choice、unknown option、缺失 option value 和混入敏感字符串参数也统一为单行固定 JSON、空 stderr 和 exit 2，不输出 usage、argv 或原始异常；`--help` 仍正常输出帮助并返回 0。
- root/manifest 解析和逐文件 SHA-256 读取错误均 fail-closed。文件读取错误只记录 manifest 内的相对路径；config I/O 与非法 JSON 分开报告。
- 每次交付 verified path/root 前都会重新哈希小型 manifest，并再次核对其 stat identity；这覆盖同 inode、同 bytes 且 NFS stat 陈旧时的内容漂移。已验证的大模型文件只重查 filesystem identity，不在每次消费前重复哈希。
- `VerifiedAssetSelection` 与其内层 `VerifiedAssetFile` 现在都要求精确类型、成功 preflight 工厂登记的对象 identity，以及与发行时一致的不可变字段 fingerprint。线程安全弱引用登记不采用 dataclass equality、不延长对象生命周期；任何字段 retarget、runtime/DB file graft、直接构造、`object.__new__`、等值伪造、实例方法改写或嵌套子类都在动态派发前硬失败并撤销 capability。
- 数据库 `required_for_runtime` schema 已收窄为 strict `Literal[False]`；当前决定中的三份 DB 只能是显式 audit-only 资产，`true` 在 manifest parse/contract 层硬拒，不再被 runtime models-only 遍历静默忽略。
- 当前生产 manifest 对声明的运行时资产已 ready；本任务没有重新读取或哈希模型权重。真实模型 load/round-trip 仍由 T020/T021 验证。

## 验证结果

- 第三轮修复以提交 `72bd362` 为基线构造 A001-only 隔离候选：asset unit/contract `84 passed in 9.27s`；完整套件 `229 passed in 24.93s`。
- manifest capability/binding 套件连续 5 轮均为 13 passed；修复了原先依赖 NFS timestamp 可见性的非确定性 replacement 测试。
- issuance registry 的字段 mutation、runtime/DB graft、32 线程并发校验与 GC 清理共 18 项测试连续 10 轮通过；等值但不同 identity 的 selection/file 均被拒绝。
- 全仓 Ruff 通过；strict Pyright 为 0 errors / 0 warnings；traceability 为 221 requirements / 221 source nodes、16 archive files、12 production modules、235 runtime config keys。
- A002 当前 scanner 对隔离候选 28 个 Python sources 只读复扫为 0 violations。
- 未使用 GPU，未创建性能占位文件。

## 未决审查

- 原独立 AI/Infra `changes requested` 已完成代码、专属证据与 A001-only 隔离验证修复，等待独立复审和主代理验收。registry revision 11 为 `C02-001`/`C03-001` 补齐 A001 CLI/readiness 实现入口；两项仍保持 `implemented`，真实模型执行仍由 T020/T021 提供 1GPU 证据后才能推进到 `verified`。
- WebDataset JSON caption 的正式 schema/manifest 仍由 D010/D011 继续锁定。
