# M034: Fixed sampling profiles and generation provenance

Status: verified complete for the CPU and one-GPU scope; multi-GPU, full-chain generation, and formal quality gates remain pending outside M034.

## Allowed paths

- `src/sakuramoon/config/schema.py`
- `src/sakuramoon/sampling/`
- `src/sakuramoon/eval/spec.py`
- `src/sakuramoon/cli/eval.py`
- `config/examples/all_options.example.toml`
- targeted tests under `tests/unit/config/`, `tests/unit/sampling/`, `tests/unit/eval/`, `tests/unit/cli/`, and `tests/gpu/`
- `tests/unit/docs/test_verify_traceability.py`
- `tools/verify_traceability.py`
- `docs/model-architecture/current/confirmed-decisions.md`
- `docs/model-architecture/progress/IMPLEMENTATION_ROADMAP.md`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/M034.md`
- `docs/model-architecture/progress/tasks/M033.md` (package-review status only)
- `docs/model-architecture/reviews/M034/test_report.json`
- `docs/model-architecture/reviews/DENSE/`

## Scope

- Expose exactly three immutable profiles: Euler-28 preview, Heun-25 plus final Euler balanced, and Heun-50 plus final Euler reference.
- Require explicit top-level generation profile selection and explicit `reference` selection for formal evaluation.
- Derive NFE from solver and steps; reject user-supplied NFE and every unregistered solver/step combination.
- Keep the linear 0-to-1 grid, FP32 state, unit noise scale, `t_eps=0.05`, x-prediction, per-branch x-to-v, and full-interval CFG 2.9 unchanged.
- Never evaluate the model again at the final `t=1` endpoint.
- Bind profile, solver, steps, derived NFE, CFG, and objective provenance into resolved/evaluation/generation identities. Legacy model-only inference is explicitly `pre_fix`.

## Excluded

- er_sde, DPM++, beta57, resolution shift, arbitrary profile composition, DDP/NCCL, multi-GPU validation, formal quality evaluation, and training long runs.

## Evidence

- Targeted CPU contracts: 95 passed.
- Expanded sampling/config/eval regression: 157 passed with three existing TorchAO deprecation warnings.
- Full unit regression: 617 passed with 21 dependency/runtime deprecation warnings.
- Real single-GPU profile smoke: 3 passed on one RTX 5090 using mounted host driver 580.105.08; all three profiles executed with FP32 state and no `t=1` model evaluation.
- Combined M033 objective plus M034 sampling GPU regression: 4 passed.
- Targeted Ruff, strict Pyright, `uv lock --check`, and diff hygiene passed.
- No DDP/NCCL, multi-GPU path, formal stage, quality run, or training long run was executed.
- Full strict Pyright, trace verifier, and 40 trace contract tests passed.
- Independent Dense Model package AI/model and Infra/performance reviews passed M030-M034 with no blocking finding.
- T041/T050/K001, checkpoint-driven full-chain generation, formal quality evaluation, and all multi-GPU gates remain pending under their owning tasks.
