# D016: Lock caption dropout configuration

Status: implementation complete in CPU scope; Data package rereview and the production
100k distribution dry run remain pending.

Dependencies: the D014 caption protocol, C001 strict schema, and G001 forward-governance
rules are complete. D016 does not depend on the pending model-packing rereview.

## Allowed paths

- `src/sakuramoon/config/schema.py`
- `config/examples/all_options.example.toml`
- `tests/unit/config/conftest.py`
- `tests/unit/config/test_schema.py`
- `tests/contracts/config/test_runtime_contract.py`
- `tests/unit/data/test_manifest_cli.py`
- `tests/unit/docs/test_verify_traceability.py`
- `docs/model-architecture/current/confirmed-decisions.md`
- `docs/model-architecture/current/open-items.md`
- `docs/model-architecture/progress/IMPLEMENTATION_ROADMAP.md`
- `docs/model-architecture/progress/traceability.toml`
- `docs/model-architecture/progress/tasks/D016.md`
- `docs/model-architecture/reviews/D016/test_report.json`

## Stable contract

- `all_condition`, `general`, `artist`, `copyright`, and `nsfw` are exactly `0.1`.
- `character` is exactly `0.2`; `candidate_source` is exactly `0.3`.
- `long_names`, `long_no_names`, `short_vibes`, `nl2`, and `nl3` are each exactly
  `0.3`.
- Every key remains required, uses TOML float syntax, and has no runtime default.

## Validation and evidence boundary

Targeted schema contracts validate the approved values and reject a drift in every
field, integer substitution, missing fields, and unknown keys. Config-dependent data
tests consume the approved example values instead of synthetic probability overrides.

This task binds configuration only. It does not modify the D014 caption algorithm or
historical evidence, and it does not claim the pending production 100k component/dropout
distribution dry run. As an ordinary CPU task it creates no independent timing artifact;
AI/model and Infra/performance conclusions remain part of the Data package rereview.
