# T042: Single-GPU raw checkpoint and model-only artifact

Status: complete for the implemented one-GPU scope; independent AI and Infra reviews passed.

## Allowed Paths

- `src/sakuramoon/checkpoint/**`
- `src/sakuramoon/conditioning/style_resampler.py`
- `src/sakuramoon/conditioning/text_mixer.py`
- `src/sakuramoon/model/dit.py`
- `tests/unit/checkpoint/**`
- `tests/gpu/checkpoint/**`
- `docs/model-architecture/progress/tasks/T042.md`
- `docs/model-architecture/reviews/T042/**`
- `docs/model-architecture/progress/time-log.jsonl`
- `docs/model-architecture/progress/traceability.toml`

## Scope

- Save the unwrapped trainable composite by deterministic canonical FQN into Safetensors shards no larger than 2 GiB.
- Save a raw single-GPU checkpoint with TorchAO optimizer state, strict optimizer schema, trainer/data/growth state, Python/NumPy/Torch/optimizer-SR RNG, payload checksums, manifest and final `COMPLETE` marker.
- Validate every required file, checksum, identity, model tensor and optimizer schema before applying state; resume accepts raw checkpoints only.
- Publish through a task-owned temporary directory and atomic rename. Save model-only artifacts without training sidecars.

## Deferred

- Checkpoint scheduling, retention and forced stage/growth points remain T043/T050.
- Partial-FQN growth migration remains T043.
- Four-rank state and barrier behavior remain pending until four RTX 5090 GPUs are available.

## One-GPU Evidence

- Twenty-one CPU tests cover strict multi-shard model-only round-trip, pre-apply checksum/file-set/full-identity/kind failures, parameter-schema identity, strict JSON versions, incomplete discovery, failed-save cleanup, RNG restoration and strict sidecar state. The full CPU unit suite passed 304 tests.
- A real TorchAO checkpoint loaded `OptimState8bit` with `weights_only=True`, restored a fresh model/optimizer and all RNG streams, and matched the uninterrupted next update exactly.
- The raw checkpoint's `model/` directory loaded independently after being copied without any training sidecars; raw resume rejected non-raw kinds.
- A full 16-layer 239-FQN composite raw checkpoint produced 5,143,055,405 payload bytes and two model shards; the largest shard was 2,145,315,208 bytes, below 2 GiB. Save/load took 20.81/6.79 seconds on a temporary overlay filesystem, not formal NVMe evidence.

## Independent Review

- AI/model correctness: PASS with no blocking findings after 81 CPU contract tests and static/traceability verification.
- Infra/performance: PASS for the single-GPU scope after 21 CPU and 9 real RTX 5090 checkpoint tests. Default NVML mismatch, real-NVMe durability/capacity, measured host RSS, scheduling/retention and all four-rank behavior remain explicit blockers or downstream work.
