# D011 review scope

D011 is part of the Data package. Independent AI/model and Infra/performance conclusions remain pending until D010-D015 package review.

Review the explicit no-default raw-field mapping, trusted D010 shard release binding,
preservation of unknown raw fields for D014, duplicate-ID report, strict exact-2,000
selection type, deterministic stratification, training exclusion, deterministic tar
streaming, exact selected-ID equality, cleanup, fsync, atomic directory publication,
and no-clobber behavior.

Do not attribute D012 cache coordination, D013 production aspect buckets, D014 caption
field interpretation, D015 WebDataset batching, the production 11M scan, a real-payload
validation artifact, or a full training zero-leak run to this task.
