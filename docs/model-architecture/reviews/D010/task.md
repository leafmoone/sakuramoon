# D010 review scope

D010 is part of the Data package. The independent AI/model and Infra/performance review is performed after D010-D015, as required by the package protocol.

For D010, review the fixed source/revision binding, strict manifest fields, remote inventory equality, streaming byte/SHA verification, atomic `.partial` publication, bounded retries, redirect credential stripping, and stable CLI errors.

Do not attribute D011 metadata isolation, D012 cache coordination/resume, D013 image processing, D014 caption logic, or D015 WebDataset batching to this task.
