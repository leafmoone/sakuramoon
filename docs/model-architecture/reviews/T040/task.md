# T040 review task

Review canonical-FQN role classification, BF16/FP32 storage, decay groups, TorchAO fallback rejection, optimizer state class/bytes/order/step, isolated SR RNG, FP32 global clip and non-finite failure. Verify the held-out validation EMA canary and serialized next-step equality. Keep all four-rank claims pending T041 and assess isolated optimizer timing without treating it as end-to-end training share.
