# Camera Coordinate Causal Audit — Forensic Tooling

This directory preserves the **final, exact scripts** that produced the
committed camera-coordinate causal audit numbers, plus the regression
contracts that lock their semantics. It is forensic/evaluation tooling:
it is NOT imported by the production runtime, does not start training, and
does not touch exposure state.

## Layout

```
dev-tools/camera_coordinate_causal/
  final_snapshot/cc_common.py    # shared constants/seeds/paths (final)
  final_snapshot/cc_stage1.py    # unit construction + arm validation (final)
  final_snapshot/cc_stage2.py    # per-unit arm scoring, worker 0/1 (final)
  final_snapshot/cc_stage2b.py   # latent-contract companion scorer (final)
  final_snapshot/cc_stage3.py    # aggregation + bootstrap + report (final, v5)
  review_contracts.py            # pure-stdlib helpers locked by tests (10A-10G)
  replay_check.py                # read-only replay of the committed numbers
  README.md                      # this file
tests/dev_tools/
  test_camera_coordinate_causal_audit_contract.py  # 20 regression tests
reports/
  camera-coordinate-causal-*                  # 5 causal-audit reports
  camera-p25-expanded-effectiveness-*         # 6 expanded-effectiveness reports
  camera-coordinate-causal-code-manifest.json # sha256 manifest of all of the above
```

## Final snapshot: which scripts generated the numbers

The committed report numbers were generated by the five scripts in
`final_snapshot/`, verified **byte-identical** (sha256 + size) to the
executed copies in the protected evidence directory
`/tmp/camera-coordinate-causal/` on salt15:

| script | sha256 | bytes |
|---|---|---|
| cc_common.py  | `63edc8ef5a62fcb1e64c202f9f12d562713512e44b618e49a719fe0527aa437b` | 10435 |
| cc_stage1.py  | `384d58e49b3c08b345525aded9485c1bb38c310d426d399aa07acf6182aa65b0` | 26491 |
| cc_stage2.py  | `e5b7ae910df0db7847fcb96d9ecfddf12ce394e383045750da695c62c7c664b9` | 11309 |
| cc_stage2b.py | `aea987d6505338b81a7e9d72084e5bef6a458b04cf41454750f347c779da2177` | 6026 |
| cc_stage3.py  | `4a855b3134b78f4478003cbdd611945b9a4e1c8e53e95827ec0cc28ea27657a6` | 51117 |

**Provenance caveat (read before trusting audit.json fields):**
`reports/camera-coordinate-causal-audit.json → provenance.script_sha256`
records the shas **at stage1-manifest time**. For `cc_stage2` and
`cc_stage3` those are the pre-fix versions (`3420f055…`, `c9d5646e…`), and
`cc_stage2b` is absent (it was added after stage1). The authoritative final
scripts are the `final_snapshot/` entries above; the full patch chain with
timestamps is preserved in the protected (non-git) file
`/tmp/camera-coordinate-causal/cc-audit-deployed-shas-rerun.txt` on salt15.
Input manifest: `stage1-manifest.json`
(sha256 `d000f58f394d42c094fe862f3fff273e42a147a0f29b380b058493c53c599485`;
units-section sha256 `7387138e9f53d85d8dae2f32a6ea0ec3b52dbc041ab514c3eba3cc3b1b950f71`).

## Audit design (as executed)

- **Base commit:** `b2443af436b268fafb2cb6c05d724e3ab0d6042c` (camera v2 p25
  entrance head; all three checkpoints run on the identical code tree).
- **Checkpoints:** PRE `output_model/g1/ckpt_116100_raw-116100-update-cadence`,
  MID `/tmp/camera-coordinate-causal/ckpts/MID/ckpt_117100_raw-117100-update-cadence`
  (protected local copy), POST
  `output_model/g1_camera_v2_p25/ckpt_118100_raw-118100-update-cadence`.
- **Units:** 2048 camera units (cohort `camera`, units 0..2047) scored as the
  causal arm population, plus 512 ordinary units as controls; the manifest
  additionally carries 64 `random_geom` and 146 `strict_identity_ordinary
  control units (2560 rows total). 8 camera units are `opp_na` (opposite arm
  not applicable) and are excluded from OPPOSITE aggregates (2040 of 2048).
- **Arms:** CORRECT (ground-truth camera), IDENTITY (zero geometry),
  OPPOSITE (sign-flipped geometry), SHUFFLED (geometry from a different unit)
  are the four required arms; HALF and OVER are interpretive extra arms (their
  bootstrap values are computed in a try/except and are not part of the
  verdict). Ordinary controls use SAME / RANDOM geometry.
- **Margin definition:** `M = Loss(wrong arm) − Loss(correct arm)` per unit,
  averaged over the unit's 4 stratum timesteps. **Sign convention (see POSTHOC
  REVIEW ERRATUM below):** a POSITIVE M means the wrong/alternative
  coordinates receive HIGHER loss than the correct ones, i.e. the correct
  coordinates are PREFERRED by the model and the wrong geometry fits WORSE.
  (An earlier version of this README stated the opposite; the implementation
  and the regression tests were correct from the start.)
- **Aggregation:** per-unit stratum means
  `cam_loss[ck][arm][i] = mean(row.loss[arm])`;
  `M_{arm}_raw = nanmean((w − c)[sel])` with `sel = opp_mask` for OPPOSITE and
  `~isnan(w)` for the other arms.
- **Bootstrap:** seed `20260906` (MASTER_SEED), `n_boot = 10000`, **units are
  the clusters** (resample units, not timesteps), paired across checkpoints.
  Final (v5) semantics: a unit is dropped from an arm's bootstrap unless its
  margin is finite at ALL checkpoints; resampling happens WITHIN the selected
  subspace (`rng.integers(0, k, (n_boot, k))`); the bootstrap call order is
  OPPOSITE → IDENTITY → SHUFFLED (the rng stream is order-sensitive, so the
  order is part of the reproducibility contract). Delta point estimates use
  the boot means; ci95 = 2.5/97.5 percentiles.

## Final results (frozen verdict — do not re-derive, replay to verify)

Core camera margins (raw, PRE/MID/POST):

- `M_OPPOSITE`:  0.00171 → 0.00203 → 0.00210;  Δ(POST−PRE) = +0.000392 [0.000244, 0.000544]
- `M_IDENTITY`: −0.00172 → … → +0.00225;  Δ(POST−PRE) = +0.003971
- `M_SHUFFLED`: Δ(POST−PRE) = +0.000756
- correct-best3 fraction: 0.243 → 0.433

Verdict and recommendation (as written in the report):

- **Behavioral:** NULL_EFFECT / WEAK — the camera geometry does not
  measurably change task behavior at the 2000U evaluation window.
- **Causal:** a small, stable, **monotonically rising internal coordinate
  dependence** — all three wrong-geometry arms show MONOTONIC_GAIN across
  PRE→MID→POST → **CASE_B**.
- **Overall verdict: INCONCLUSIVE** — at the end of the window the
  dependence trajectory was still rising (no terminal plateau observed).
- **RECOMMENDATION: NO_MORE_EXPOSURE_YET** — do not extend exposure on the
  strength of this result alone.

**Framing rule (must be preserved in any downstream writing):** this audit
measures *internal representation dependence on camera coordinates*, not a
causal proof that the camera feature improves generation. It must NOT be
summarized as "camera proven successful".

## Reports and manifest

`reports/camera-coordinate-causal-code-manifest.json` (generated by
`gen_manifest.py` from the on-disk files, schema_version 1) records the
sha256 + byte size of:

- the five `final_snapshot/` scripts (and their `/tmp` source identity),
- the five causal-audit reports (audit.md / audit.json / metrics.json /
  units.csv / copy-report.md),
- the six expanded-effectiveness reports (audit.md / audit.json /
  metrics.json / points.csv / prompts.csv / copy-report.md),
- the input stage1 manifest sha256 (file + units section),
- the bootstrap seed and the stale-provenance note above.

Full 64-character shas live in that manifest — do not retype them by hand
elsewhere.

## Verification (how to re-check this handoff)

All commands run in the review worktree with the salt15 DTK venv
(`source /opt/dtk/env.sh` then
`/sakuramoon-runtime/sakuramoon-dtk-venv/bin/python`):

1. `python -m py_compile dev-tools/camera_coordinate_causal/final_snapshot/*.py`
   (five compile = snapshot is valid Python),
2. `python -m pytest tests/dev_tools/test_camera_coordinate_causal_audit_contract.py -q`
   (expect 20 passed, 0 skipped, 0 xfailed),
3. `ruff check dev-tools/camera_coordinate_causal/review_contracts.py
   dev-tools/camera_coordinate_causal/replay_check.py
   dev-tools/camera_coordinate_causal/gen_manifest.py
   tests/dev_tools/test_camera_coordinate_causal_audit_contract.py`
   (repo ruff config: `target-version = "py311"`, default rule set). The
   frozen `final_snapshot/*.py` are byte-exact forensic copies and are never
   edited to satisfy lint; a ruff pass over them is informational only.
4. `python dev-tools/camera_coordinate_causal/replay_check.py`
   (expect `REPLAY = PASS`; exit 2 = mismatch, exit 3 = ledgers absent →
   SKIPPED_NOT_REQUIRED). The replay recomputes the 9 point estimates and the
   3-arm bootstrap block from the FINAL stage2 ledgers and compares them
   EXACTLY (float64 equality after the JSON round-trip) against the committed
   audit.json.

## Known bug history (evidence-based)

Nine patches were applied to the audit scripts between the first deployment
and the final successful run. The authoritative timestamped patch chain is
the protected non-git file `/tmp/camera-coordinate-causal/cc-audit-deployed-shas-rerun.txt`
on salt15. The intermediate crash logs were rotated (nohup overwrite) before
the successful run, so the list below is reconstructed from the patch
comments, the run1 log, and the rerun evidence file:

| # | stage | bug | fix | evidence |
|---|---|---|---|---|
| B1 | stage1 | arm-validation gate tolerance `>0.0` too strict for DCU-vs-CPU fp32 (opposite arm maxdiff 1.19e-07, 1 ulp) | tolerance `>1e-5` | run1 `FAILED_ARM_VALIDATION` in stage1.log lines 103-104 |
| B2 | stage2 | latent passed as 4-D `(1,128,16,16)` bf16; PackedDiT requires a 3-D `[C,H,W]` tuple (dit.py ~710 ValueError) | `squeeze(0)` at 4 latent-handling sites | v2/v3 patch comments in the rerun evidence file |
| B3 | stage2b | same 3-D latent contract class | same squeeze fix | cc_stage2b v2 patch comment |
| B4 | stage3 | `sel` bracket placed outside `nanmean` → IndexError | moved inside the margin expression | v2 patch comment |
| B5 | stage3 | bootstrap resampled GLOBAL unit indices (2047) into the 2040-row selected subspace → IndexError | resample WITHIN the selected subspace (`rng.integers(0, k, (n_boot, k))`) | v3 patch comment |
| B6 | stage3 | wrong key name in the determinism probe | corrected to the recorded key | v4 patch comment |
| B7 | stage3 | 8 `opp_na` units leaked NaN into strata tables | v5 finite-mask: drop non-finite units per aggregate | v5 patch comment |

**Bug classes A and B (locked as contracts):** class A = checkpoint-key
collision in the per-unit result ledger (PRE/MID/POST rows overwriting each
other); class B = seed/variant collapse (three seeds counted as one variant
cardinality). The final dataset shows the correct behavior (unique
`prompt_id=validation-<hex16>` ids, no observed collapse), and no direct
crash artifact from these classes survives in the final run; the
regression tests (10A `aggregate_ledger_units` / `split_base_variant` +
`variant_cardinality`) therefore lock the CORRECT ledger-keying and
variant-cardinality semantics of the final code, which is what this handoff
depends on.

Contract helpers and their test mapping: 10A ledger keying, 10B variant
cardinality, 10C distinct identity hashes, 10D latent 3-D contract,
10E bootstrap checkpoint isolation + subspace bounds + paired structure,
10F band/stratum masks, 10G margin sign convention.

## POSTHOC REVIEW ERRATUM (added by the posthoc review branch)

This README correction is **documentation erratum only**; the numerical sign
implementation and the regression tests were already correct. Historical raw
numbers are unchanged.

1. **Margin sign prose (fixed above).** `M = Loss(wrong) − Loss(correct)`.
   A POSITIVE M means the wrong geometry has HIGHER loss, therefore the
   correct coordinates are preferred. The earlier prose ("wrong geometry fits
   better") was directionally inverted.
2. **Historical verdict short-circuit.** The historical `INCONCLUSIVE` verdict
   was forced by the stage3 numerics gate
   `numerics_ok = determinism AND max(M_SAME_maxabs over ck) < 1e-6`; the
   actual ordinary SAME maxabs (PRE/MID/POST = 9.16e-05 / 1.57e-04 / 8.12e-05)
   all exceed 1e-6, so the effect logic (rising + CI exclusion) never executed.
   The `harness numerics OK=True` line in the historical audit.md is derived
   from the determinism-probe non-empty check only — it is NOT the `numerics_ok`
   gate boolean (decision state and reported state were inconsistent).
3. **Edge naming.** The historical overall `L/C/R` labels are normalized-offset
   tertiles (`norm_offset < 1/3 / [1/3, 2/3) / >= 2/3`) and do NOT by
   themselves mean physical LEFT/CENTER/RIGHT. The posthoc report uses the
   universal `START/CENTER/END` naming plus orientation-specific physical
   labels (horizontal LEFT/CENTER/RIGHT, vertical TOP/CENTER/BOTTOM), with the
   axis direction verified from `src/sakuramoon/data/camera_viewport.py`
   (`normalized_offset = left/available` horizontal, `top/available` vertical).
   The historical "right-edge" finding is re-stated as an **END-offset
   regression candidate**.
4. **Scope of supersession.** The new posthoc report
   (`reports/camera-coordinate-causal-posthoc-*`) supersedes only the
   verdict/numerics INTERPRETATION of the historical audit. It does not
   supersede, regenerate, or alter any historical evidence file
   (`final_snapshot/**`, the five historical causal reports, the six expanded
   effectiveness reports). Historical raw numbers remain exactly as committed.

## Red lines (kept by this handoff)

- No force/reset/rewrite; the review branch is exactly `b2443af` + 2 commits.
- Only `dev-tools/camera_coordinate_causal/**`, `tests/dev_tools/**` and
  `reports/camera-coordinate-causal-*` / `reports/camera-p25-expanded-effectiveness-*`
  are tracked in this branch; nothing else is modified.
- No training, no resume, no exposure of any kind.
- `/tmp/camera-coordinate-causal/` on salt15 (~15 GB of ledgers, checkpoints
  and logs) is protected evidence: never delete, move, or "clean".
- The C2 worktree (`sakuramoon-camera-v2-c2`) is untouched by this handoff.
- Single-file size cap respected (largest tracked file: units.csv,
  676,722 bytes).
